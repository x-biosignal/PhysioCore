% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{logStep}
\alias{logStep}
\title{Record an analysis step in the provenance log}
\usage{
logStep(
  x,
  step,
  params = list(),
  package = NA_character_,
  version = NA_character_
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}.}

\item{step}{Character scalar naming the operation (e.g. \code{"filterSignals"}).}

\item{params}{Named list of parameters to record.}

\item{package, version}{Optional originating package name and version.}
}
\value{
\code{x} with the step appended (the input is unchanged; a modified
copy is returned).
}
\description{
Appends a timestamped, agent-attributed PROV activity. Call this from any
operation that returns a modified \code{PhysioExperiment}.
}
\examples{
pe <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(20), 10, 2)), samplingRate = 100)
pe <- logStep(pe, "filterSignals", params = list(low = 1, high = 40))
provenance(pe)
}
