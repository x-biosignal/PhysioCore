% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-mvar.R
\name{mvarFit}
\alias{mvarFit}
\title{Fit an MVAR model (user-facing)}
\usage{
mvarFit(
  X,
  order = NULL,
  method = c("ols", "yulewalker", "nuttall-strand"),
  max_order = 20L,
  criterion = c("bic", "aic")
)
}
\arguments{
\item{X}{A numeric matrix (\code{time x channels}).}

\item{order}{Integer order, or \code{NULL} (default) to select automatically.}

\item{method}{Estimator (see \code{\link{.fitMVAR}}).}

\item{max_order}{Maximum order for automatic selection (default: 20).}

\item{criterion}{Selection criterion when \code{order} is \code{NULL}
(default "bic").}
}
\value{
An object of class \code{"mvar"}: a list with \code{A}, \code{Sigma},
\code{order}, \code{method}, and \code{n_channels}.
}
\description{
Convenience wrapper around \code{\link{.fitMVAR}} that optionally selects the
model order automatically via \code{\link{mvarOrderSelect}}.
}
\examples{
set.seed(1)
X <- matrix(rnorm(600), ncol = 3)
fit <- mvarFit(X, order = 2)
dim(fit$A)
}
\seealso{
\code{\link{mvarTransfer}}, \code{\link{mvarOrderSelect}}
}
