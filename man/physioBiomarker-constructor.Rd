% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker.R
\name{physioBiomarker}
\alias{physioBiomarker}
\title{Construct a reliability-characterised biomarker}
\usage{
physioBiomarker(
  value,
  name,
  unit = NA_character_,
  ci = NULL,
  reference_range = NULL,
  reliability = list(),
  provenance = list(),
  interpretation = NA_character_
)
}
\arguments{
\item{value}{Numeric scalar value.}

\item{name}{Character biomarker name (e.g. \code{"DAR"}).}

\item{unit}{Character measurement unit (default \code{NA}).}

\item{ci}{Numeric length-2 confidence interval, or \code{NULL} for none.}

\item{reference_range}{Numeric length-2 published reference (normal) range,
or \code{NULL} for none. Interpreted as a 95 percent reference interval when
computing the printed normative percentile.}

\item{reliability}{Named list of reliability indices, typically \code{icc},
\code{sem}, and \code{mdc} (see \code{\link[=icc]{icc()}}, \code{\link[=sem]{sem()}}, \code{\link[=mdc]{mdc()}}).}

\item{provenance}{Named list describing how the value was computed, typically
\code{assay}, \code{band}, \code{method}, and \code{software_version}.}

\item{interpretation}{Optional character interpretation label.}
}
\value{
A \link{PhysioBiomarker} object.
}
\description{
Builds a \link{PhysioBiomarker} carrying not just the value but the metadata a
clinical reader needs: a confidence interval, a published reference range,
reliability indices (ICC / SEM / MDC, per COSMIN reporting), and the
measurement provenance (assay, band, method, software version). The value
prints with its CI and, when a reference range is supplied, its normative
percentile; \code{\link[BiocGenerics:as.data.frame]{BiocGenerics::as.data.frame()}} flattens every field for tabular reporting.
}
\examples{
bm <- physioBiomarker(
  value = 2.35, name = "DAR", unit = "ratio",
  ci = c(1.90, 2.80), reference_range = c(0.5, 1.2),
  reliability = list(icc = 0.82, sem = 0.15, mdc = 0.42),
  provenance = list(assay = "psd", band = "delta/alpha",
                    method = "welch", software_version = "1.0.0")
)
bm
as.data.frame(bm)
}
\seealso{
\code{\link[=PhysioBiomarker]{PhysioBiomarker()}}, \code{\link[=normativeLookup]{normativeLookup()}}, \code{\link[=is.PhysioBiomarker]{is.PhysioBiomarker()}},
\code{\link[=icc]{icc()}}, \code{\link[=sem]{sem()}}, \code{\link[=mdc]{mdc()}}
}
