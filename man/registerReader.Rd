% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{registerReader}
\alias{registerReader}
\alias{getReader}
\alias{availableReaders}
\alias{unregisterReader}
\title{Register and dispatch file readers}
\usage{
registerReader(format, fn, ext = NULL, overwrite = FALSE)

getReader(format)

availableReaders()

unregisterReader(format)
}
\arguments{
\item{format}{Case-insensitive format key (e.g. \code{"brainvision"}).}

\item{fn}{The reader function; its first argument should be a file path.}

\item{ext}{Optional character vector of file extensions handled (no dot).}

\item{overwrite}{If \code{FALSE} (default), registering an existing \code{format}
errors; \code{TRUE} replaces it (use this in a package \code{.onLoad}).}
}
\value{
\code{registerReader()} invisibly returns \code{fn}; \code{getReader()} returns the
registered function; \code{availableReaders()} returns a data.frame of
\code{format}/\code{ext} (the \code{format} column holds normalized, lower-cased keys, and
the frame has zero rows when nothing is registered); \code{unregisterReader()}
invisibly returns \code{TRUE} if a reader was removed.
}
\description{
Register and dispatch file readers
}
\examples{
registerReader("demo_csv", function(file, ...) read.csv(file, ...),
               ext = "csv", overwrite = TRUE)
availableReaders()
r <- getReader("DEMO_CSV")   # case-insensitive
unregisterReader("demo_csv")
}
\seealso{
Other plugin-api: 
\code{\link{physio-registry}},
\code{\link{registerOperation}()},
\code{\link{registerWriter}()}
}
\concept{plugin-api}
