% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-mvar.R
\name{mvarOrderSelect}
\alias{mvarOrderSelect}
\title{MVAR model-order selection by AIC / BIC}
\usage{
mvarOrderSelect(
  X,
  max_order = 20L,
  criterion = c("bic", "aic"),
  method = c("ols", "yulewalker", "nuttall-strand")
)
}
\arguments{
\item{X}{A numeric matrix (\code{time x channels}).}

\item{max_order}{Maximum order to consider (default: 20).}

\item{criterion}{\code{"bic"} (default) or \code{"aic"}.}

\item{method}{Estimator passed to \code{\link{.fitMVAR}} (default "ols").}
}
\value{
A list with the selected \code{order}, the chosen \code{criterion},
and a \code{data.frame} \code{criteria} of \code{order}, \code{aic},
\code{bic}.
}
\description{
Fits MVAR models over a range of orders and returns the order minimising the
Akaike (AIC) or Bayesian (BIC) information criterion. For an m-channel model
of order p with n effective samples the criteria are
\code{n log det(Sigma_p) + 2 m^2 p} (AIC) and
\code{n log det(Sigma_p) + log(n) m^2 p} (BIC).
}
\seealso{
\code{\link{.fitMVAR}}, \code{\link{mvarFit}}
}
