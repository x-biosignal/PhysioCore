% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-mvar.R
\name{.mvarSpectral}
\alias{.mvarSpectral}
\title{MVAR spectral factorization (transfer function and spectral density)}
\usage{
.mvarSpectral(A, Sigma, freqs, sr)
}
\arguments{
\item{A}{Coefficient array (\code{channels x channels x order}).}

\item{Sigma}{Residual covariance (\code{channels x channels}).}

\item{freqs}{Numeric vector of frequencies in Hz.}

\item{sr}{Sampling rate in Hz.}
}
\value{
A list of complex arrays (\code{channels x channels x length(freqs)}):
\code{H} (transfer function), \code{S} (spectral density), \code{A} (the
\eqn{\bar{A}(f)} matrices), plus \code{frequencies}.
}
\description{
Turns fitted MVAR coefficients into the frequency-domain quantities used by
DTF, PDC and spectral Granger causality. At frequency \eqn{f},
\eqn{\bar{A}(f) = I - \sum_k A_k e^{-2\pi i f k / sr}}, the transfer function
is \eqn{H(f) = \bar{A}(f)^{-1}}, and the spectral density is
\eqn{S(f) = H(f)\,\Sigma\,H(f)^{*}}.
}
\seealso{
\code{\link{mvarTransfer}}, \code{\link{.fitMVAR}}
}
\keyword{internal}
