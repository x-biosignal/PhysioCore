% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MultiRatePhysioExperiment-class.R
\name{MultiRatePhysioExperiment}
\alias{MultiRatePhysioExperiment}
\title{Construct a MultiRatePhysioExperiment}
\usage{
MultiRatePhysioExperiment(
  streams = list(),
  ...,
  clock = NULL,
  t0 = 0,
  reference_rate = NULL,
  offsets = NULL
)
}
\arguments{
\item{streams}{A named list of \code{PhysioExperiment} streams (or pass them
as named \code{...} arguments).}

\item{...}{Additional named \code{PhysioExperiment} streams.}

\item{clock}{Optional pre-built master-clock list. If \code{NULL} (default) a
clock is built from \code{t0}, \code{reference_rate} and \code{offsets}.}

\item{t0}{Origin timestamp in seconds (default 0).}

\item{reference_rate}{Reference sampling rate in Hz for
\code{\link{alignStreams}}. Defaults to the highest stream rate.}

\item{offsets}{Named numeric of per-stream start offsets in seconds relative
to \code{t0} (default 0 for every stream).}
}
\value{
A \code{MultiRatePhysioExperiment}.
}
\description{
Construct a MultiRatePhysioExperiment
}
\examples{
kin <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(100 * 3), 100, 3)), samplingRate = 100)
emg <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(2000 * 2), 2000, 2)), samplingRate = 2000)
mr <- MultiRatePhysioExperiment(kinematics = kin, emg = emg)
streamRates(mr)
}
\seealso{
\code{\link{streamRates}}, \code{\link{resampleToCommon}}
}
