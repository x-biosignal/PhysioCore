% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{registerWriter}
\alias{registerWriter}
\alias{getWriter}
\alias{availableWriters}
\alias{unregisterWriter}
\title{Register and dispatch file writers}
\usage{
registerWriter(format, fn, ext = NULL, overwrite = FALSE)

getWriter(format)

availableWriters()

unregisterWriter(format)
}
\arguments{
\item{format}{Case-insensitive format key (e.g. \code{"brainvision"}).}

\item{fn}{The writer function; its first arguments should be the object to
write and a file path.}

\item{ext}{Optional character vector of file extensions handled (no dot).}

\item{overwrite}{If \code{FALSE} (default), registering an existing \code{format}
errors; \code{TRUE} replaces it (use this in a package \code{.onLoad}).}
}
\value{
\code{registerWriter()} invisibly returns \code{fn}; \code{getWriter()} returns the
registered function; \code{availableWriters()} returns a data.frame of
\code{format}/\code{ext} (the \code{format} column holds normalized, lower-cased keys, and
the frame has zero rows when nothing is registered); \code{unregisterWriter()}
invisibly returns \code{TRUE} if a writer was removed.
}
\description{
Register and dispatch file writers
}
\examples{
registerWriter("demo_csv", function(x, file, ...) write.csv(x, file, ...),
               ext = "csv", overwrite = TRUE)
availableWriters()
unregisterWriter("demo_csv")
}
\seealso{
Other plugin-api: 
\code{\link{physio-registry}},
\code{\link{registerOperation}()},
\code{\link{registerReader}()}
}
\concept{plugin-api}
