% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{getEvents}
\alias{getEvents}
\title{Get events from a PhysioExperiment object}
\usage{
getEvents(x, type = NULL)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{type}{Optional character vector of event types to filter.}
}
\value{
A PhysioEvents object or DataFrame of events.
}
\description{
Get events from a PhysioExperiment object
}
\examples{
# Create PhysioExperiment with events
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000), nrow = 100)),
  samplingRate = 100
)
events <- PhysioEvents(
  onset = c(1, 2, 3),
  type = c("stimulus", "response", "stimulus")
)
pe <- setEvents(pe, events)

# Get all events
getEvents(pe)

# Get only stimulus events
getEvents(pe, type = "stimulus")
}
\references{
Delorme A, Makeig S (2004). "EEGLAB: an open source toolbox for analysis
of single-trial EEG dynamics." \emph{Journal of Neuroscience Methods},
134(1), 9-21.

Oostenveld R, Fries P, Maris E, Schoffelen JM (2011). "FieldTrip: Open
source software for advanced analysis of MEG, EEG, and invasive
electrophysiological data." \emph{Computational Intelligence and
Neuroscience}, 2011, 156869.
}
\seealso{
\code{\link{setEvents}} for attaching events,
\code{\link{addEvents}} for appending events,
\code{\link{removeEvents}} for removing events,
\code{\link{nEvents}} for event count
}
