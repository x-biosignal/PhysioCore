% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{addProvenance}
\alias{addProvenance}
\title{Append a provenance step, capturing an optional seed}
\usage{
addProvenance(
  pe,
  step,
  params = list(),
  seed = NULL,
  package = NA_character_,
  version = NA_character_
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} object.}

\item{step}{Character step / activity name.}

\item{params}{Named list of parameters recorded with the step.}

\item{seed}{Optional integer seed captured in the entry parameters.}

\item{package, version}{Optional producing-package name and version.}
}
\value{
\code{pe} with the entry appended to its provenance log.
}
\description{
A thin convenience over \code{\link{logStep}} that records an append-only
provenance entry on the object's metadata log (never dropping prior entries),
optionally capturing an integer \code{seed} in the entry parameters so the
step is reproducible under a fixed seed.
}
\examples{
pe <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(20), 10, 2)), samplingRate = 100)
pe <- addProvenance(pe, "conformalInterval", seed = 42)
provenance(pe)
}
\seealso{
\code{\link{logStep}}, \code{\link{provenance}}
}
