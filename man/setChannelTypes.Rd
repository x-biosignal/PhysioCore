% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{setChannelTypes}
\alias{setChannelTypes}
\title{Set channel types}
\usage{
setChannelTypes(x, types)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{types}{Named character vector or list mapping channel names/indices to types.
If unnamed, applies types in order.}
}
\value{
Modified PhysioExperiment object.
}
\description{
Assigns types (EEG, EMG, EOG, etc.) to channels.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "EOG1", "EMG1", "Oz")),
  samplingRate = 100
)

# Set all channels to same type
pe <- setChannelTypes(pe, "EEG")

# Set specific channel types by name
pe <- setChannelTypes(pe, c(EOG1 = "EOG", EMG1 = "EMG"))
}
\references{
Oostenveld R, Fries P, Maris E, Schoffelen JM (2011). "FieldTrip: Open
source software for advanced analysis of MEG, EEG, and invasive
electrophysiological data." \emph{Computational Intelligence and
Neuroscience}, 2011, 156869.
}
\seealso{
\code{\link{getChannelsByType}} for querying channels by type,
\code{\link{channelInfo}} for full channel metadata,
\code{\link{setChannelUnits}} for assigning physical units
}
