% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifecycle.R
\name{deprecate_physio}
\alias{deprecate_physio}
\title{Emit a Physio-ecosystem deprecation warning or error}
\usage{
deprecate_physio(
  when,
  what,
  with = NULL,
  details = NULL,
  severity = c("warn", "stop"),
  id = NULL
)
}
\arguments{
\item{when}{Version string in which the deprecation was introduced, e.g.
\code{"0.3.0"}.}

\item{what}{String naming the deprecated function or argument, e.g.
\code{"old_fn()"} or \code{"old_fn(arg)"}. Prefix with the package
(\code{"PhysioECG::old_fn()"}) to attribute it explicitly.}

\item{with}{Optional string naming the replacement, e.g. \code{"new_fn()"}.}

\item{details}{Optional character vector of extra guidance shown to the user.}

\item{severity}{\code{"warn"} (default) to emit a deprecation warning, or \code{"stop"}
to raise a defunct error.}

\item{id}{Optional deprecation id used to de-duplicate warnings within a
session; ignored when \code{severity = "stop"}.}
}
\value{
Called for its side effect (a warning or an error); invisibly returns
\code{NULL}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}
}
\details{
A thin, ecosystem-wide wrapper over \code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_warn()}} and
\code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_stop()}} so every Physio package signals deprecations the
same way. Use \code{severity = "warn"} during the deprecation window and switch to
\code{severity = "stop"} once the item is defunct. See the ecosystem
\code{DEPRECATION.md} policy for the lifecycle stages and the minimum window.
}
\examples{
old_fn <- function(x) {
  deprecate_physio("0.3.0", "old_fn()", with = "new_fn()")
  x
}
# Warnings are shown once per session by default; force them for testing with
# options(lifecycle_verbosity = "warning").
}
\seealso{
\code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_warn()}}, \code{\link[lifecycle:deprecate_soft]{lifecycle::deprecate_stop()}}
}
\concept{lifecycle}
