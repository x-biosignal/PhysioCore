% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{appendProvenance}
\alias{appendProvenance}
\title{Append a provenance activity}
\usage{
appendProvenance(
  x,
  entry = NULL,
  activity = NULL,
  params = list(),
  input_assay = NA_character_,
  output_assay = NA_character_,
  agent = NA_character_,
  software_version = NA_character_,
  package = NA_character_
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}.}

\item{entry}{Either a pre-built entry \code{list} (low-level form) or a
character scalar naming the \code{activity} (equivalent to passing
\code{activity=}).}

\item{activity}{Character scalar naming the PROV activity (the operation).}

\item{params}{Named list of parameters to record.}

\item{input_assay, output_assay}{The assay(s) the activity used / generated
(PROV \code{used} / \code{wasGeneratedBy}).}

\item{agent}{The responsible agent (defaults to \code{user@host}).}

\item{software_version, package}{Software agent name and version.}
}
\value{
\code{x} with the activity appended to its provenance log.
}
\description{
Appends one PROV activity to the object's audit trail. Two forms are
supported: a high-level form that builds a full PROV-O record from named
fields, and a low-level form that appends a pre-built entry list.
}
\examples{
pe <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(20), 10, 2)), samplingRate = 100)
pe <- appendProvenance(pe, activity = "filterSignals",
                       params = list(low = 1, high = 40),
                       input_assay = "raw", output_assay = "filtered")
provenance(pe)$activity
}
