% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{recordActivity}
\alias{recordActivity}
\title{Record a PROV activity around an operation}
\usage{
recordActivity(
  x,
  activity,
  expr,
  params = list(),
  input_assay = NA_character_,
  output_assay = NA_character_,
  agent = NA_character_,
  software_version = NA_character_,
  package = NA_character_
)
}
\arguments{
\item{x}{The input \code{PhysioExperiment}.}

\item{activity}{Character scalar naming the PROV activity.}

\item{expr}{An expression, evaluated lazily, that returns the modified object
(e.g. \code{filterSignals(x, low = 1)}).}

\item{params}{Named list of parameters to record.}

\item{input_assay, output_assay, agent, software_version, package}{As in
\code{\link{appendProvenance}}.}
}
\value{
The object returned by \code{expr}, with the timed activity appended.
}
\description{
Evaluates \code{expr} (an operation that transforms \code{x} and returns a
\code{PhysioExperiment}), timing it to fill the PROV \code{startedAtTime} /
\code{endedAtTime}, and appends one activity to the result. The input's
provenance log is carried onto the result if the operation dropped it.
}
\examples{
pe <- PhysioExperiment(
  S4Vectors::SimpleList(raw = matrix(rnorm(20), 10, 2)), samplingRate = 100)
scale2 <- function(p) { SummarizedExperiment::assay(p, "raw") <-
  SummarizedExperiment::assay(p, "raw") * 2; p }
pe <- recordActivity(pe, "rescale", scale2(pe), params = list(gain = 2))
provenance(pe)$activity
}
\seealso{
\code{\link{withProvenance}}, \code{\link{logStep}}
}
