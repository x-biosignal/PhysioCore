# Subjects of a PhysioCohort

Subjects of a PhysioCohort

## Usage

``` r
subjects(x)

# S4 method for class 'PhysioCohort'
subjects(x)

subjects(x) <- value

# S4 method for class 'PhysioCohort'
subjects(x) <- value
```

## Arguments

- x:

  A `PhysioCohort`.

- value:

  A named list / SimpleList of `PhysioLongitudinal` (setter).

## Value

`subjects()` a `SimpleList` of `PhysioLongitudinal`.
