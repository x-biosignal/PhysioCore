# Subject-level metadata

Subject-level metadata

## Usage

``` r
subjectData(x)

# S4 method for class 'PhysioLongitudinal'
subjectData(x)

subjectData(x) <- value

# S4 method for class 'PhysioLongitudinal'
subjectData(x) <- value
```

## Arguments

- x:

  A `PhysioLongitudinal`.

- value:

  A one-row subject `DataFrame` (setter).

## Value

`subjectData()` the subject `DataFrame`.
