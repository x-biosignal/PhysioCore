# EventQuery class for composable event filtering

EventQuery class for composable event filtering

## Slots

- `source`:

  PhysioExperiment object

- `filters`:

  List of filter functions to apply

- `resolved`:

  Cached resolved events (NULL if not yet resolved)
